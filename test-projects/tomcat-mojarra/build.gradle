description = 'JoinFaces Tomcat-Mojarra Test Project'

dependencies {
    implementation(project(':joinfaces-starters:jsf-spring-boot-starter')) {
        exclude module: "richfaces-spring-boot-starter"
    }
}
